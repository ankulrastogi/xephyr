package com.own.transaction.processor.channel;

import com.own.transaction.enums.ChannelType;

public class ChannelSelectorFactory {

	public static ChannelSelector getChannelSelector(ChannelType type)
	{
		return null;
	}
}
